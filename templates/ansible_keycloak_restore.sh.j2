#! /bin/sh
/usr/bin/docker exec {{ keycloak_postgres_container_name }} /bin/bash -c "PGPASSWORD={{ keycloak_db_password }} pg_restore --format=c --clean --username={{ keycloak_db_user }} --dbname={{ keycloak_db_name }} /var/lib/postgresql/data/{{ keycloak_backup_file }}"
